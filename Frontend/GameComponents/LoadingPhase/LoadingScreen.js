import { Text, TouchableOpacity, View } from 'react-native';

export default function LoadingScreen({currentGame, setCurrentGame}) {

    return (
      <View style={{}}>
        <Text>Loading</Text>
      </View>
    );
  }